codecov:
  require_ci_to_pass: true
  notify:
    wait_for_ci: true

coverage:
  precision: 2
  round: down
  range: "70...100"
  
  status:
    project:
      default:
        target: 70%
        threshold: 5%        # Allow 5% decrease before failing
        if_ci_failed: error
        informational: false
    
    patch:
      default:
        target: 80%          # Reasonable bar for new code
        threshold: 15%       # Allow up to 15% deviation (65-95% acceptable)
        informational: true  # Show status but don't block PRs
        only_pulls: true     # Only enforce on pull requests

comment:
  layout: "reach,diff,flags,tree,footer"
  behavior: default
  require_changes: false
  require_base: false
  require_head: true

ignore:
  - "**/__tests__/**"
  - "**/__mocks__/**"
  - "**/*.d.ts"
  - "**/index.ts"
  - "scripts/**"
  - "migrations/**"
  - "**/*.config.ts"
  - "**/*.config.js"
