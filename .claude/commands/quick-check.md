---
description: Run quick validation (lint + typecheck)
---

Run a quick validation check of the codebase to ensure linting and type checking pass. This is faster than the full check and good for iterative development.

Commands to run:
```bash
npm run lint && npm run typecheck
```
