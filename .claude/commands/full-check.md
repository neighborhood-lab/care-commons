---
description: Run full validation (lint + typecheck + test + build)
---

Run the complete validation suite before creating a PR. This runs all quality gates that will be checked in CI.

Command to run:
```bash
./scripts/check.sh
```
